---
title: "ai procedures"
description: "Source input schemas, access wrappers, and handler links for ai."
---

These entries are extracted from the source, not hand-invented endpoint examples. Read [API conventions](/reference/api-overview) before calling them.

## ai.getSettings

**Query · `protectedProcedure`**

[Handler and return value](https://github.com/CassieMarie0728/jekyll-forge/blob/c889b55402f374d832b2412d9ce1a32c5fae06a7/server/routers/ai.ts#L55).

No input schema is declared for this procedure.

## ai.updateSettings

**Mutation · `protectedProcedure`**

[Handler and return value](https://github.com/CassieMarie0728/jekyll-forge/blob/c889b55402f374d832b2412d9ce1a32c5fae06a7/server/routers/ai.ts#L59).

```typescript
z.object({
        enabled: z.boolean().optional(),
        temperature: z.number().min(0).max(100).optional(),
        maxTokens: z.number().min(100).max(8000).optional(),
        systemPrompt: z.string().optional(),
        brandVoicePrompt: z.string().optional(),
        safetyPrompt: z.string().optional(),
        streaming: z.boolean().optional(),
        defaultLanguage: z.string().optional(),
        defaultTone: z.enum(WRITING_TONES).optional(),
        budgetLimitCents: z.number().optional().nullable(),
      })
```

## ai.generate

**Mutation · `protectedProcedure`**

[Handler and return value](https://github.com/CassieMarie0728/jekyll-forge/blob/c889b55402f374d832b2412d9ce1a32c5fae06a7/server/routers/ai.ts#L78).

```typescript
z.object({
        task: z.string(),
        userPrompt: z.string().optional(),
        selectedText: z.string().optional(),
        postMarkdown: z.string().optional(),
        frontMatter: z.record(z.string(), z.unknown()).optional(),
        tone: z.string().optional(),
        voiceProfileId: z.number().optional(),
      })
```

## ai.getUsageStats

**Query · `protectedProcedure`**

[Handler and return value](https://github.com/CassieMarie0728/jekyll-forge/blob/c889b55402f374d832b2412d9ce1a32c5fae06a7/server/routers/ai.ts#L143).

No input schema is declared for this procedure.

## Source and verification

Source-reviewed against app revision `c889b55` on September 20, 2026. This label establishes implementation evidence, not live acceptance of every operation. See [Current state](/reference/current-state).

[server/routers/ai.ts](https://github.com/CassieMarie0728/jekyll-forge/blob/c889b55402f374d832b2412d9ce1a32c5fae06a7/server/routers/ai.ts).
